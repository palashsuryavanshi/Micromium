# Micromium overlay ignores — never commit a Chromium checkout here.
# Anchored (leading /) so overlay dirs like rust/src/ are NOT ignored.
/src/
/out/
.chromium_src/
*.pyc
__pycache__/
.vs/
*.ninja
*.gn.bak
depot_tools/
