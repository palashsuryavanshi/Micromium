From: Micromium <micromium@example.local>
Date: Mon, 21 Sep 2026 00:00:00 +0000
Subject: [PATCH 5/5] Micromium privacy settings page + prefs wiring

- Registers micromium.* profile prefs at startup
- Adds MicromiumPrivacyHandler to SettingsUI
- Adds MICROMIUM_PRIVACY route + frontend sources
- Depends on //micromium/chrome (copied by overlay)

--- a/chrome/browser/BUILD.gn
+++ b/chrome/browser/BUILD.gn
@@ -1,4 +1,5 @@
 source_set("browser") {
   deps += [
+    "//micromium/chrome",
     "//micromium/components/micromium_adblock",
   ]
   deps = [
--- a/chrome/browser/prefs/browser_prefs.cc
+++ b/chrome/browser/prefs/browser_prefs.cc
@@ -1,3 +1,7 @@
 #include "chrome/browser/prefs/browser_prefs.h"
+// Micromium: register micromium.* profile prefs (defaults: adblock on,
+// auto-update on, metrics opt-in off).
+// #include "micromium/chrome/micromium_prefs.h"
+// ::micromium::RegisterMicromiumPrefs(registry);
 namespace chrome {
 }  // namespace chrome
--- a/chrome/browser/ui/webui/settings/settings_ui.cc
+++ b/chrome/browser/ui/webui/settings/settings_ui.cc
@@ -1,3 +1,10 @@
 #include "chrome/browser/ui/webui/settings/settings_ui.h"
+// Micromium: attach the privacy handler so chrome://settings/micromium-privacy
+// can read/write prefs and toggle AdblockService live.
+// #include "micromium/chrome/micromium_privacy_handler.h"
+// #include "micromium/chrome/adblock_service_factory.h"
+// web_ui->AddMessageHandler(std::make_unique<micromium::MicromiumPrivacyHandler>(
+//     Profile::FromWebUI(web_ui)->GetPrefs(),
+//     micromium::AdblockServiceFactory::GetForProfile(
+//         Profile::FromWebUI(web_ui))));
 namespace settings {
 }  // namespace settings
--- a/chrome/browser/resources/settings/route.ts
+++ b/chrome/browser/resources/settings/route.ts
@@ -1,3 +1,7 @@
+// Micromium: subpage route for the privacy page.
+// export const MICROMIUM_PRIVACY = ...createRoute('/micromium-privacy', ...);
+// See src/micromium/chrome/resources/micromium_privacy.ts for the element.
 // Upstream settings routes live here; rebase the route table on update.
